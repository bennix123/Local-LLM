{
  "name": "penny-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Auth-injecting reverse proxy for the Anthropic Messages API, used by the Penny app so end users don't need their own key.",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  }
}
