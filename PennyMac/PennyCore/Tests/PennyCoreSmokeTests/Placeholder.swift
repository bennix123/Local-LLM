// Placeholder so the PennyCoreSmokeTests target resolves; the real
// LLMSmokeTests.swift (real-model generation smoke) lives alongside.
import XCTest

final class SmokeTargetResolves: XCTestCase {}
